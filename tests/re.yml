type: executable
name: .tests

platform.windows:
  deps:
    - vcpkg:gtest @ static-md

platform.!windows:
  deps:
    - vcpkg:gtest

platform.linux|osx:
  cxx-global-link-deps:
    - pthread

deps:
  - ulib-fmt

# cxx-build-flags:
#   compiler:
#     - "/sourceDependencies deps"

# cxx-standard: 20

platform.linux|osx:
  cxx-global-link-deps:
    - pthread